#!/bin/bash

# ==================================================================
# This script is used to deploy the PyFrag application on a High performance computer (HPC) cluster.
# The following is required for deploying the PyFrag application:
# - Install the Python package manager "uv" if it is not already installed
# - Create a virtual environment (Python) with the pyfrag package installed through `uv sync`
# ==================================================================
